1. What do we mean when we say that MySQL has a client-server architecture?
Client programs interact with the server.  You can write your own as well as the supplied programs.

2. Use the monitor help to look up information on the SELECT statement. (We’ll discuss SELECT in detail in Chapter 5.)
Tells you how to ask the server questions.  Addresses the basics of the select statement.

3. What is the difference between using the monitor in interactive mode and using
the monitor in batch mode?
The batch takes in a file with successive commands and executes them one at a time without stopping except for errors.
Interactive mode allows you to execute one statement at a time.

4. What do the monitor user, password, and database options do?
Specify input parameters.